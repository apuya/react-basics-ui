/**
 * Checkbox - Form control for binary choices or multiple selections.
 *
 * @example
 * ```tsx
 * import { Checkbox } from 'react-basics-ui';
 *
 * <Checkbox label="Accept terms" onChange={handleChange} />
 * <Checkbox label="Select all" indeterminate={someSelected} />
 * ```
 *
 * @module Checkbox
 */
export { Checkbox } from './Checkbox';
export type { CheckboxProps, CheckboxSize } from './Checkbox.types';
